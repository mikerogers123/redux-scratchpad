import { Action } from 'redux';


// export class ManagedAction<T> implements Action<T> {
//     constructor(private type: string) { }

//     public matches(action: Action): action is ManagedAction<T> {
//         return action.type === this.type;
//     }
// }
